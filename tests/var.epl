var noval: Int
var full: Int = 5
var notype = 6
var unit = {}

let lnoval: Int //<ModelError not initialized
let lfull: Int = full
let lnotype = notype

var noval: Int //<ModelError already defined

fn main() {
   print(noval) //<InterpreterError not initialized
   assert(ieq(full, 5))
   assert(ieq(notype, 6))
   assert(ieq(lfull, 5))
   assert(ieq(lnotype, 6))

   full = 7
   assert(ieq(full, 7))
   
   lfull = 7 //<ModelError immutable
   assert(ieq(lfull, 5))

   unit = {}
}
