enum Nat {
     one,
     two,
     three,
}
enum Bool {
     true,
     false,
}
type T = Bool
type Test = (Bool, Bool)

let onevar = one
var twovar : Nat = two

fn first(a: Nat, b: Nat) {
   a
}
fn get_one() {
   one
}
fn main() -> Nat {
  let a = first(one, two)
  three
}