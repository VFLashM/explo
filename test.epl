let first = fn(a: Int, b: Int) -> Int {
    a
}

let second = fn(a: Int, b: Int) -> Int {
    first(b, a)
}

let Some = Int

let pass = fn(v: Some) -> Some {
    v
}

let ignore = fn(v: Some) {}

var gvar = 4

let show = fn(i: Int) {
    print(i)
}

let main = fn() {
    var a = first(1, pass(2))
    a = 5
    gvar = 6
    show(a)
    pass(a)
}
