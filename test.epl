enum Nat {
     one,
     two,
     three,
}
enum Bool {
     true,
     false,
}
type T = Bool
type Test = (Bool, Bool)

let onevar = one
var twovar : Nat = two

fn first(a: Nat, b: Nat) -> Nat {
   a
}
fn get_one() {
   one
}
fn main() -> Nat {
  print(add(1,5))
  let a = first(one, two)
  twovar = a
  print(twovar)
  twovar
}